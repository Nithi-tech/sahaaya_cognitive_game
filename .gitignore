# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
server/*.db
server/*.db-wal
server/*.db-shm

# Third-party reference material only (AGPL-3.0, read-only reference during
# development — see docs/LICENSE_DECISION.md) — never distributed with the
# product, so it must never be committed.
extracted-games/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
